# Environment Variables for Production
# Copy this file to .env.local for local development
# Add these variables to your Netlify dashboard for production

# API Configuration
VITE_API_URL=https://your-backend-url.com/api
VITE_APP_ENV=production

# AI Provider API Keys (Add these in Netlify dashboard)
# OPENAI_API_KEY=your_openai_api_key_here
# CLAUDE_API_KEY=your_claude_api_key_here
# GEMINI_API_KEY=your_gemini_api_key_here

# Database (for backend deployment)
# DATABASE_PATH=./database/meetings.json
# PORT=3001
# DEFAULT_AI_PROVIDER=gemini

# Instructions:
# 1. For Netlify frontend deployment:
#    - Add VITE_API_URL and VITE_APP_ENV to Netlify environment variables
# 2. For backend deployment (Railway/Render):
#    - Add all AI API keys and database configuration
# 3. Update VITE_API_URL to point to your deployed backend URL